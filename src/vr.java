import java.net.URI;
import java.nio.file.Path;
import java.util.Optional;

public record vr(ww a, Optional<Path> b, Optional<URI> c) {
   public vr(ww $$0) {
      this($$0, Optional.empty(), Optional.empty());
   }
}
