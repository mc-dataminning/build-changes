import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface duu {
   @Nullable
   eis a(eik var1);

   void a(eik var1, eis var2);

   LongSet b(eik var1);

   void a(eik var1, long var2);

   Map<eik, LongSet> h();

   void b(Map<eik, LongSet> var1);
}
