import com.mojang.serialization.MapCodec;

public interface eng<SP extends enf> {
   eng<end> a = a("random_spread", end.a);
   eng<enc> b = a("concentric_rings", enc.a);

   MapCodec<SP> codec();

   private static <SP extends enf> eng<SP> a(String $$0, MapCodec<SP> $$1) {
      return kd.a(lz.P, $$0, () -> $$1);
   }
}
