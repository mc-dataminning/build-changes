import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fdv implements ese {
   @Nullable
   @Override
   public esd a(int $$0) {
      return fef.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
