import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fad implements eon {
   @Nullable
   @Override
   public eom a(int $$0) {
      return fam.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
