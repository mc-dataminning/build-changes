import com.mojang.authlib.GameProfile;

public record app(GameProfile a, int b, aop c, boolean d) {
   public static app a(GameProfile $$0, boolean $$1) {
      return new app($$0, 0, aop.a(), $$1);
   }
}
