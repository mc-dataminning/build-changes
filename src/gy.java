public class gy<T extends du<T>> implements gn<T> {
   private static final gy<? extends du<?>> a = (gy<? extends du<?>>)(new gy<>());

   public static <T extends du<T>> gn<T> a() {
      return (gn<T>)a;
   }

   @Override
   public void execute(go<T> $$0, gq $$1) {
      $$1.a();
      $$1.b();
   }
}
