import java.util.List;
import java.util.UUID;

public interface dko {
   dko a = ($$0, $$1, $$2) -> $$0.a($$2x -> $$2x.dm().a($$1, (double)$$2) && !$$2x.f() && !$$2x.P_()).stream().map(bno::cw).toList();
   dko b = ($$0, $$1, $$2) -> {
      enn $$3 = new enn($$1).g((double)$$2);
      return $$0.a(bnu.aH, $$3, bog::bx).stream().map(bno::cw).toList();
   };

   List<UUID> detect(aov var1, hz var2, int var3);
}
