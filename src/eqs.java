import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface eqs {
   Codec<eqs> c = lz.n.q().dispatch(eqs::a, eqt::codec);

   @Nullable
   ul a(azu var1, @Nullable ul var2);

   eqt<?> a();
}
