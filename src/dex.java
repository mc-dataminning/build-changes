public class dex extends dep {
   @Override
   protected aew<dnj<?, ?>> a(asc $$0, boolean $$1) {
      if ($$0.a(10) == 0) {
         return $$1 ? ox.I : ox.n;
      } else {
         return $$1 ? ox.C : ox.g;
      }
   }
}
