import com.mojang.serialization.Codec;
import java.util.Optional;

public interface eav {
   Codec<eav> c = lz.t.q().dispatch(eav::a, eaw::a);
   zh<wu, eav> d = zf.a(ma.ab).b(eav::a, eaw::b);

   Optional<ezy> a(dfm var1);

   eaw<? extends eav> a();
}
