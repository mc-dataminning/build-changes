import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bnc extends bne {
   bnd d();

   @Nullable
   bmx.a c(String var1);

   Set<Pair<String, bok>> e();
}
