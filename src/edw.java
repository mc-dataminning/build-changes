import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface edw {
   Codec<edw> c = kd.p.q().dispatch(edw::a, edx::codec);

   @Nullable
   so a(auw var1, @Nullable so var2);

   edx<?> a();
}
