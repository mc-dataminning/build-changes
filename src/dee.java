import com.mojang.serialization.Codec;
import java.util.List;

public interface dee<T extends dek> {
   Codec<dee<?>> a = mg.r.q().dispatch(dee::a, deo::a);
   Codec<alf<dee<?>>> b = alf.a(mh.bv);
   yw<wj, dee<?>> c = yu.a(mh.ah).b(dee::a, deo::b);

   boolean a(T var1, djm var2);

   czn a(T var1, jh.a var2);

   default boolean am_() {
      return false;
   }

   default boolean i() {
      return true;
   }

   default String j() {
      return "";
   }

   deo<? extends dee<T>> a();

   dep<? extends dee<T>> b();

   ded al_();

   default List<dfk> g() {
      return List.of();
   }

   deh h();
}
