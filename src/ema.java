import com.mojang.serialization.MapCodec;

public interface ema<P extends elz> {
   ema<ely> a = a("always_true", ely.a);
   ema<elw> b = a("linear_pos", elw.a);
   ema<ell> c = a("axis_aligned_linear_pos", ell.a);

   MapCodec<P> codec();

   static <P extends elz> ema<P> a(String $$0, MapCodec<P> $$1) {
      return jv.a(lp.q, $$0, () -> $$1);
   }
}
