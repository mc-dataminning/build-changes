import java.util.List;

public enum fnt {
   a,
   b;

   private static final List<fnt> c = List.of(values());
   private static final iu.b d = iu.a(kd.as);

   public static io<fnt> a() {
      return new io<>(c).a(a, d);
   }
}
