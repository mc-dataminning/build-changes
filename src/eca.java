import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface eca<T> extends AutoCloseable {
   CompletableFuture<ebv<T>> a(dgn var1);

   void a(ebv<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
