import java.util.UUID;
import java.util.function.Consumer;
import javax.annotation.Nullable;

public interface dln<T extends dle> {
   @Nullable
   T a(int var1);

   @Nullable
   T a(UUID var1);

   Iterable<T> a();

   <U extends T> void a(dll<T, U> var1, asf<U> var2);

   void a(eju var1, Consumer<T> var2);

   <U extends T> void a(dll<T, U> var1, eju var2, asf<U> var3);
}
