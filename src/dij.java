import java.util.List;
import java.util.UUID;

public interface dij {
   dij a = ($$0, $$1, $$2) -> $$0.a($$2x -> $$2x.dm().a($$1, (double)$$2) && !$$2x.f() && !$$2x.P_()).stream().map(blp::cw).toList();
   dij b = ($$0, $$1, $$2) -> {
      elh $$3 = new elh($$1).g((double)$$2);
      return $$0.a(blt.aG, $$3, bmf::bx).stream().map(blp::cw).toList();
   };

   List<UUID> detect(amz var1, hx var2, int var3);
}
