import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bdc extends bde {
   bdd d();

   @Nullable
   bcx.a c(String var1);

   Set<Pair<String, bei>> e();
}
