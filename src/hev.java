import com.mojang.serialization.MapCodec;
import javax.annotation.Nullable;

public interface hev<T> {
   void a(@Nullable T var1, cxf var2, fgr var3, gmx var4, int var5, int var6, boolean var7);

   @Nullable
   T b(cxh var1);

   public interface a {
      @Nullable
      hev<?> a(gfy var1);

      MapCodec<? extends hev.a> a();
   }
}
