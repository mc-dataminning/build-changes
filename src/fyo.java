import java.util.List;

public enum fyo implements daw {
   a(dbg.c, dbg.a, dbg.d, dbg.b),
   b(dbg.e, dbg.f, dbg.g),
   c(dbg.h, dbg.i),
   d(dbg.j);

   private final List<dbh> e;

   private fyo(final dbh... $$0) {
      this.e = List.of($$0);
   }

   public List<dbh> a() {
      return this.e;
   }
}
