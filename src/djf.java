import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface djf<T> extends AutoCloseable {
   CompletableFuture<dja<T>> a(cpe var1);

   void a(dja<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
