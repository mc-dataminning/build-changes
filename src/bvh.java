import com.mojang.datafixers.kinds.App;
import java.util.function.Function;
import java.util.function.Predicate;

public class bvh {
   public static <E extends btc> buw<E> a(Predicate<E> $$0, ccg<?> $$1) {
      return byi.a((Function<byi.b<E>, ? extends App<byi.c<E>, byl<E>>>)($$2 -> $$2.group($$2.b($$1)).apply($$2, $$1xx -> ($$2x, $$3, $$4) -> {
               if ($$0.test((E)$$3)) {
                  $$1xx.b();
                  return true;
               } else {
                  return false;
               }
            })));
   }
}
