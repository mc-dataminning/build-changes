import com.mojang.datafixers.kinds.App;
import java.util.function.Function;

public class cls {
   public static bve<cli> a() {
      return byq.a((Function<byq.b<cli>, ? extends App<byq.c<cli>, byt<cli>>>)($$0 -> $$0.group($$0.c(cco.ad)).apply($$0, $$0x -> ($$0xx, $$1, $$2) -> {
               if (!$$1.eY().e() && !$$1.eY().a(cun.vs)) {
                  clj.a($$1, true);
                  return true;
               } else {
                  return false;
               }
            })));
   }
}
