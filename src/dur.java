import com.mojang.serialization.Codec;
import java.util.stream.Stream;

public abstract class dur {
   public static final Codec<dur> b = jd.V.q().dispatch(dur::b, dus::codec);

   public abstract Stream<gw> a_(dup var1, ase var2, gw var3);

   public abstract dus<?> b();
}
