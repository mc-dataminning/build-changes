import com.mojang.serialization.Codec;
import java.util.stream.Stream;

public abstract class dup {
   public static final Codec<dup> b = jb.V.q().dispatch(dup::b, duq::codec);

   public abstract Stream<gw> a_(dun var1, asc var2, gw var3);

   public abstract duq<?> b();
}
