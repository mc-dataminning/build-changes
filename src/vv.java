import java.net.URI;
import java.nio.file.Path;
import java.util.Optional;

public record vv(wz a, Optional<Path> b, Optional<URI> c) {
   public vv(wz $$0) {
      this($$0, Optional.empty(), Optional.empty());
   }
}
