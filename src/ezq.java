import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class ezq implements eoc {
   @Nullable
   @Override
   public eob a(int $$0) {
      return ezz.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
