import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface ecs {
   @Nullable
   erb a(eqt var1);

   void a(eqt var1, erb var2);

   LongSet b(eqt var1);

   void a(eqt var1, long var2);

   Map<eqt, LongSet> h();

   void b(Map<eqt, LongSet> var1);
}
