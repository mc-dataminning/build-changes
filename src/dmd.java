import javax.annotation.Nullable;

public interface dmd {
   @Nullable
   dtx a(ji var1, dwv var2);

   @Nullable
   default <T extends dtx> dty<T> a(dgg $$0, dwv $$1, dtz<T> $$2) {
      return null;
   }

   @Nullable
   default <T extends dtx> ebt a(arc $$0, T $$1) {
      return $$1 instanceof ebt.b<?> $$2 ? $$2.c() : null;
   }
}
