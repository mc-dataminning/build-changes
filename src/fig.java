import java.util.List;

public enum fig {
   a,
   b;

   private static final List<fig> c = List.of(values());
   private static final ht.b d = ht.a(jc.ap);

   public static hm<fig> a() {
      return new hm<>(c).a(a, d);
   }
}
