import java.util.List;

public enum fip {
   a,
   b;

   private static final List<fip> c = List.of(values());
   private static final hs.b d = hs.a(jb.ap);

   public static hl<fip> a() {
      return new hl<>(c).a(a, d);
   }
}
