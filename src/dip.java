import java.util.List;
import java.util.UUID;

public interface dip {
   dip a = ($$0, $$1, $$2) -> $$0.a($$2x -> $$2x.dm().a($$1, (double)$$2) && !$$2x.f() && !$$2x.P_()).stream().map(blu::cw).toList();
   dip b = ($$0, $$1, $$2) -> {
      eln $$3 = new eln($$1).g((double)$$2);
      return $$0.a(bly.aG, $$3, bmk::bx).stream().map(blu::cw).toList();
   };

   List<UUID> detect(and var1, hx var2, int var3);
}
