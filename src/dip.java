import com.mojang.serialization.MapCodec;

public class dip extends dgq implements ctq {
   public static final MapCodec<dip> c = b(dip::new);

   @Override
   public MapCodec<dip> a() {
      return c;
   }

   protected dip(dsz.d $$0) {
      super($$0);
   }

   @Override
   public bsx m() {
      return bsx.f;
   }
}
