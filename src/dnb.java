import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dnb<T> extends AutoCloseable {
   CompletableFuture<dmw<T>> a(csw var1);

   void a(dmw<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
