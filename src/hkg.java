import com.mojang.serialization.MapCodec;
import javax.annotation.Nullable;

public interface hkg<T> {
   void a(@Nullable T var1, czy var2, flq var3, gsc var4, int var5, int var6, boolean var7);

   @Nullable
   T b(daa var1);

   public interface a {
      @Nullable
      hkg<?> a(glc var1);

      MapCodec<? extends hkg.a> a();
   }
}
