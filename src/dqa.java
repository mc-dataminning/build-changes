import com.mojang.serialization.Codec;
import java.util.Optional;

public interface dqa {
   Codec<dqa> c = kf.v.q().dispatch(dqa::a, dqb::a);
   xo<vb, dqa> d = xm.a(kg.V).b(dqa::a, dqb::b);

   Optional<enz> a(cvr var1);

   dqb<? extends dqa> a();
}
