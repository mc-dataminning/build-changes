import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class evb implements ejs {
   @Nullable
   @Override
   public ejr a(int $$0) {
      return evk.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
