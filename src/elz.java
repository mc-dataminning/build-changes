import it.unimi.dsi.fastutil.ints.IntSet;
import javax.annotation.Nullable;

public interface elz extends AutoCloseable {
   @Override
   default void close() {
   }

   @Nullable
   default ely a(int $$0) {
      return null;
   }

   IntSet a();
}
