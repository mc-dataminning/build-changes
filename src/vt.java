import java.net.URI;
import java.nio.file.Path;
import java.util.Optional;

public record vt(wy a, Optional<Path> b, Optional<URI> c) {
   public vt(wy $$0) {
      this($$0, Optional.empty(), Optional.empty());
   }
}
