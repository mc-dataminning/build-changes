import java.util.Set;

public interface cov {
   Set<akj<? extends jw<? extends cov>>> bA = Set.of(lr.K, lr.f, lr.z, lr.U, lr.ac, lr.V);

   coy i();

   default boolean a(coy $$0) {
      return this.i().a($$0);
   }
}
