public interface cmn extends cma<bgj> {
   @Override
   default cmd<?> f() {
      return cmd.g;
   }

   @Override
   default boolean a(int $$0, int $$1) {
      return $$0 >= 3 && $$1 >= 1;
   }

   @Override
   default cix h() {
      return new cix(csm.ob);
   }

   boolean a(cix var1);

   boolean b(cix var1);

   boolean c(cix var1);
}
