import java.util.concurrent.CompletableFuture;
import javax.annotation.Nullable;

@FunctionalInterface
public interface sr {
   sr a = ($$0, $$1) -> CompletableFuture.completedFuture($$1);

   CompletableFuture<sw> decorate(@Nullable aig var1, sw var2);
}
