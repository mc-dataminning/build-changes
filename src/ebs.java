import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public class ebs implements ebu {
   private static final ebs b = new ebs();
   public static final Codec<ebs> a = Codec.unit(b);

   @Override
   public sd a(atw $$0, @Nullable sd $$1) {
      return new sd();
   }

   @Override
   public ebv<?> a() {
      return ebv.a;
   }
}
