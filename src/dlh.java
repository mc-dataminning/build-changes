import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dlh<T> extends AutoCloseable {
   CompletableFuture<dlc<T>> a(crm var1);

   void a(dlc<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
