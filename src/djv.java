import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface djv<T> extends AutoCloseable {
   CompletableFuture<djq<T>> a(cqg var1);

   void a(djq<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
