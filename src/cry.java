import java.util.Optional;
import java.util.function.Predicate;

public interface cry {
   boolean a(ht var1, Predicate<dgw> var2);

   boolean b(ht var1, Predicate<ecg> var2);

   <T extends der> Optional<T> a(ht var1, det<T> var2);

   ht a(dmf.a var1, ht var2);
}
