import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface ejy {
   Codec<ejy> c = kt.p.q().dispatch(ejy::a, ejz::codec);

   @Nullable
   to a(axt var1, @Nullable to var2);

   ejz<?> a();
}
