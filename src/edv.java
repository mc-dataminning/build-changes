import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public class edv implements edw {
   public static final edv a = new edv();
   public static final Codec<edv> b = Codec.unit(a);

   @Nullable
   @Override
   public so a(auw $$0, @Nullable so $$1) {
      return $$1;
   }

   @Override
   public edx<?> a() {
      return edx.b;
   }
}
