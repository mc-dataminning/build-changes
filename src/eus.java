import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface eus {
   Codec<eus> c = mf.n.q().dispatch(eus::a, eut::codec);

   @Nullable
   tx a(azt var1, @Nullable tx var2);

   eut<?> a();
}
