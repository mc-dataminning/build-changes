import java.util.List;

public enum fxx {
   a,
   b;

   private static final List<fxx> c = List.of(values());
   private static final jw.b d = jw.a(lp.aw);

   public static jp<fxx> a() {
      return new jp<>(c).a(a, d);
   }
}
