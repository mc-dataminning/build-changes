import com.mojang.serialization.MapCodec;

public class dbl extends czm implements coe {
   public static final MapCodec<dbl> c = b(dbl::new);

   @Override
   public MapCodec<dbl> a() {
      return c;
   }

   protected dbl(dli.d $$0) {
      super($$0);
   }

   @Override
   public bnx g() {
      return bnx.f;
   }
}
