import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface eko {
   Codec<eko> c = lc.p.q().dispatch(eko::a, ekp::codec);

   @Nullable
   ty a(ayd var1, @Nullable ty var2);

   ekp<?> a();
}
