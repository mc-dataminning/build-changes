import java.util.List;

public enum gek implements dej {
   a(det.c, det.a, det.d, det.b),
   b(det.e, det.f, det.g),
   c(det.h, det.i),
   d(det.j);

   private final List<deu> e;

   private gek(final deu... $$0) {
      this.e = List.of($$0);
   }

   public List<deu> a() {
      return this.e;
   }
}
