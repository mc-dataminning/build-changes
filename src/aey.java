import java.util.List;

public enum aey {
   a,
   b,
   c,
   d;

   private static final List<aey> e = List.of(values());
   private static final ht.b f = ht.a(jc.ap);

   public static hm<aey> a() {
      return new hm<>(e).a(a, f);
   }
}
