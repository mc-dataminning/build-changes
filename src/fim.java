import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fim implements ewv {
   @Nullable
   @Override
   public ewu a(int $$0) {
      return fiw.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
