public enum bes {
   a("pathfinding"),
   b("event-loops"),
   c("mailboxes"),
   d("ticking"),
   e("jvm"),
   f("chunk rendering"),
   g("chunk rendering dispatching"),
   h("cpu"),
   i("gpu");

   private final String j;

   private bes(String $$0) {
      this.j = $$0;
   }

   public String a() {
      return this.j;
   }
}
