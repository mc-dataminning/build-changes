import java.util.UUID;

public interface grm {
   void a(UUID var1, grm.b var2);

   void a(UUID var1, grm.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
