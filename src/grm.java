import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface grm {
   Collection<alf> f();

   void a(Function<alf, grm> var1);

   @Nullable
   grb a(grf var1, Function<gre, gpa> var2, grj var3, alf var4);
}
