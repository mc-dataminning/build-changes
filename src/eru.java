import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface eru {
   Codec<eru> c = mb.n.q().dispatch(eru::a, erv::codec);

   @Nullable
   tq a(azg var1, @Nullable tq var2);

   erv<?> a();
}
