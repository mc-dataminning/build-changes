import java.util.Set;

public interface chi {
   Set<ahc<? extends it<? extends chi>>> bx = Set.of(ke.F, ke.f, ke.u, ke.P);

   chl m();

   default boolean a(chl $$0) {
      return this.m().a($$0);
   }
}
