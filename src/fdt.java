import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fdt implements esc {
   @Nullable
   @Override
   public esb a(int $$0) {
      return fed.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
