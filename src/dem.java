import java.util.Optional;
import java.util.function.Predicate;

public interface dem {
   boolean a(je var1, Predicate<duo> var2);

   boolean b(je var1, Predicate<eqp> var2);

   <T extends drs> Optional<T> a(je var1, dru<T> var2);

   je a(eak.a var1, je var2);
}
