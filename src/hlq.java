import java.util.UUID;

public interface hlq {
   void a(UUID var1, hlq.b var2);

   void a(UUID var1, hlq.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
