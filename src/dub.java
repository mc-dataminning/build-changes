import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dub<T> extends AutoCloseable {
   CompletableFuture<dtw<T>> a(czb var1);

   void a(dtw<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
