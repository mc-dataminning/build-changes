import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fri implements fdu {
   @Nullable
   @Override
   public fdt a(int $$0) {
      return frs.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
