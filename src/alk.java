import java.util.List;

public enum alk {
   a,
   b,
   c,
   d;

   private static final List<alk> e = List.of(values());
   private static final kb.b f = kb.a(lu.az);

   public static ju<alk> a() {
      return new ju<>(e).a(a, f);
   }
}
