import java.util.List;

public enum alk {
   a,
   b,
   c,
   d;

   private static final List<alk> e = List.of(values());
   private static final jw.b f = jw.a(lp.aw);

   public static jp<alk> a() {
      return new jp<>(e).a(a, f);
   }
}
