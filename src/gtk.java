import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface gtk {
   Collection<akt> f();

   void a(Function<akt, gtk> var1);

   @Nullable
   gsz a(gtd var1, Function<gtc, gqy> var2, gth var3, akt var4);
}
