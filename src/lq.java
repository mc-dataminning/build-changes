import java.util.function.Consumer;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class lq implements ln {
   @Override
   public void a(ix.a $$0, Consumer<af> $$1) {
      af $$2 = ln.a("adventure/root");
      ls.a($$2, $$1, Stream.concat(ls.a.stream(), Stream.of(bqr.m, bqr.l)).collect(Collectors.toList()));
   }
}
