public interface egg {
   egi a();

   int b();

   boolean a(ars var1, jf<ege> var2, ege.a var3, ffq var4);

   default egg.a c() {
      return egg.a.a;
   }

   public static enum a {
      a,
      b;
   }

   public interface b<T extends egg> {
      T c();
   }
}
