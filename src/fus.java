import java.util.List;

public enum fus {
   a,
   b;

   private static final List<fus> c = List.of(values());
   private static final jb.b d = jb.a(kt.at);

   public static iu<fus> a() {
      return new iu<>(c).a(a, d);
   }
}
