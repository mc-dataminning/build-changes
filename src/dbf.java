import java.util.Optional;
import java.util.function.Predicate;

public interface dbf {
   boolean a(io var1, Predicate<drd> var2);

   boolean b(io var1, Predicate<emw> var2);

   <T extends doi> Optional<T> a(io var1, dok<T> var2);

   io a(dwv.a var1, io var2);
}
