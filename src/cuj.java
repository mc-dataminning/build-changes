import java.util.function.Supplier;
import javax.annotation.Nullable;

public interface cuj extends cud {
   long C();

   default boolean f_(hx $$0) {
      return true;
   }

   default void a(@Nullable Supplier<String> $$0) {
   }
}
