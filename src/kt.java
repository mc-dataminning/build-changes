import java.util.function.Consumer;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class kt implements kq {
   @Override
   public void a(il.b $$0, Consumer<af> $$1) {
      af $$2 = kq.a("adventure/root");
      kv.a($$2, $$1, Stream.concat(kv.a.stream(), Stream.of(bnw.m)).collect(Collectors.toList()));
   }
}
