import java.util.Optional;
import java.util.function.Predicate;

public interface cwk {
   boolean a(ib var1, Predicate<dme> var2);

   boolean b(ib var1, Predicate<ehr> var2);

   <T extends djl> Optional<T> a(ib var1, djn<T> var2);

   ib a(drq.a var1, ib var2);
}
