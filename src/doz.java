import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface doz<T> extends AutoCloseable {
   CompletableFuture<dou<T>> a(cuu var1);

   void a(dou<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
