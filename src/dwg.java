import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dwg<T> extends AutoCloseable {
   CompletableFuture<dwb<T>> a(dbg var1);

   void a(dwb<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
