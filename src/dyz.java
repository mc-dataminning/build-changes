import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dyz<T> extends AutoCloseable {
   CompletableFuture<dyu<T>> a(ddp var1);

   void a(dyu<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
