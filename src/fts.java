import java.util.Map;
import java.util.stream.Collectors;

public class fts implements AutoCloseable {
   public static final int a = ftp.F().stream().mapToInt(ftp::G).sum();
   private final Map<ftp, epw> b = ftp.F().stream().collect(Collectors.toMap($$0 -> (ftp)$$0, $$0 -> new epw($$0.G())));

   public epw a(ftp $$0) {
      return this.b.get($$0);
   }

   public void a() {
      this.b.values().forEach(epw::g);
   }

   public void b() {
      this.b.values().forEach(epw::h);
   }

   @Override
   public void close() {
      this.b.values().forEach(epw::i);
   }
}
