import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class exq implements eme {
   @Nullable
   @Override
   public emd a(int $$0) {
      return exz.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
