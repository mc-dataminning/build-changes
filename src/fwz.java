import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fwz implements fik {
   @Nullable
   @Override
   public fij a(int $$0) {
      return fxj.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
