import com.mojang.datafixers.kinds.App;
import java.util.function.Function;

public class cns {
   public static bxb<cni> a() {
      return can.a((Function<can.b<cni>, ? extends App<can.c<cni>, caq<cni>>>)($$0 -> $$0.group($$0.c(cel.ad)).apply($$0, $$0x -> ($$0xx, $$1, $$2) -> {
               if (!$$1.eY().f() && !$$1.eY().a(cwq.vK)) {
                  cnj.a($$0xx, $$1, true);
                  return true;
               } else {
                  return false;
               }
            })));
   }
}
