import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fok implements fcj {
   @Nullable
   @Override
   public fci a(int $$0) {
      return fov.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
