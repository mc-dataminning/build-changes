import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dwf<T> extends AutoCloseable {
   CompletableFuture<dwa<T>> a(dbf var1);

   void a(dwa<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
