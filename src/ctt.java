import java.util.function.Supplier;
import javax.annotation.Nullable;

public interface ctt extends ctn {
   long B();

   default boolean f_(hv $$0) {
      return true;
   }

   default void a(@Nullable Supplier<String> $$0) {
   }
}
