import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dnj<T> extends AutoCloseable {
   CompletableFuture<dne<T>> a(cte var1);

   void a(dne<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
