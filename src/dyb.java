import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface dyb {
   @Nullable
   emf a(elx var1);

   void a(elx var1, emf var2);

   LongSet b(elx var1);

   void a(elx var1, long var2);

   Map<elx, LongSet> h();

   void b(Map<elx, LongSet> var1);
}
