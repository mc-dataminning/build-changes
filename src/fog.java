import java.util.List;

public enum fog {
   a,
   b;

   private static final List<fog> c = List.of(values());
   private static final iu.b d = iu.a(kd.as);

   public static io<fog> a() {
      return new io<>(c).a(a, d);
   }
}
