import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class erk implements egm {
   @Nullable
   @Override
   public egl a(int $$0) {
      return ert.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
