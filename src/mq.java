import java.util.List;
import java.util.concurrent.CompletableFuture;

public class mq {
   public static mn a(mm $$0, CompletableFuture<ju.a> $$1) {
      return new mn($$0, $$1, List.of(new mv(), new ms(), new mr(), new mt(), new mu()));
   }
}
