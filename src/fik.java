import java.util.List;

public enum fik {
   a,
   b;

   private static final List<fik> c = List.of(values());
   private static final hs.b d = hs.a(jb.ap);

   public static hl<fik> a() {
      return new hl<>(c).a(a, d);
   }
}
