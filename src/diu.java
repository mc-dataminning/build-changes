import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface diu<T> extends AutoCloseable {
   CompletableFuture<dip<T>> a(cot var1);

   void a(dip<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
