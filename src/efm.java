import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface efm {
   Codec<efm> c = kf.p.q().dispatch(efm::a, efn::codec);

   @Nullable
   sw a(awo var1, @Nullable sw var2);

   efn<?> a();
}
