import java.util.function.Consumer;

@FunctionalInterface
public interface guc {
   guc a = ($$0, $$1) -> {
   };

   default guc decorate(Consumer<gug.a> $$0) {
      return ($$1, $$2) -> this.send($$1, $$2x -> {
            $$2.accept($$2x);
            $$0.accept($$2x);
         });
   }

   void send(gud var1, Consumer<gug.a> var2);
}
