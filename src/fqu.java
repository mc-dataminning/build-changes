import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fqu implements fer {
   @Nullable
   @Override
   public feq a(int $$0) {
      return fre.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
