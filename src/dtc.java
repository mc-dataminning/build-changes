import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dtc<T> extends AutoCloseable {
   CompletableFuture<dsx<T>> a(cye var1);

   void a(dsx<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
