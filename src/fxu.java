import java.util.List;

public enum fxu implements dcb {
   a(dbi.c, dbi.a, dbi.d, dbi.b),
   b(dbi.e, dbi.f, dbi.g),
   c(dbi.h, dbi.i),
   d(dbi.j);

   private final List<dbi> e;

   private fxu(final dbi... $$0) {
      this.e = List.of($$0);
   }

   public List<dbi> a() {
      return this.e;
   }
}
