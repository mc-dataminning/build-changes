import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bil extends bin {
   bim d();

   @Nullable
   big.a c(String var1);

   Set<Pair<String, bjq>> e();
}
