import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bpq extends bpt {
   bpr d();

   @Nullable
   bpl.a c(String var1);

   Set<Pair<String, brb>> e();
}
