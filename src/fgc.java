import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fgc implements eul {
   @Nullable
   @Override
   public euk a(int $$0) {
      return fgm.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
