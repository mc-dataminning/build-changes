import it.unimi.dsi.fastutil.ints.IntSet;
import javax.annotation.Nullable;

public interface enw extends AutoCloseable {
   @Override
   default void close() {
   }

   @Nullable
   default env a(int $$0) {
      return null;
   }

   IntSet a();
}
