import java.util.UUID;

public interface hht {
   void a(UUID var1, hht.b var2);

   void a(UUID var1, hht.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
