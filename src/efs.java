import com.mojang.serialization.Codec;
import java.util.Optional;

public interface efs {
   Codec<efs> c = mg.t.q().dispatch(efs::a, eft::a);
   yw<wj, efs> d = yu.a(mh.ac).b(efs::a, eft::b);

   Optional<fex> a(djh var1);

   eft<? extends efs> a();
}
