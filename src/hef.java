import java.util.UUID;

public interface hef {
   void a(UUID var1, hef.b var2);

   void a(UUID var1, hef.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
