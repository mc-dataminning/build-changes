import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface esm {
   Codec<esm> c = ma.n.q().dispatch(esm::a, esn::codec);

   @Nullable
   ux a(bam var1, @Nullable ux var2);

   esn<?> a();
}
