import java.util.List;

public enum gkt {
   a,
   b;

   private static final List<gkt> c = List.of(values());
   private static final jt.b d = jt.a(mg.aF);

   public static jm<gkt> a() {
      return new jm<>(c).a(a, d);
   }
}
