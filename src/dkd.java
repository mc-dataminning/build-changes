import java.util.Optional;
import java.util.function.Predicate;

public interface dkd {
   boolean a(iv var1, Predicate<ebe> var2);

   boolean b(iv var1, Predicate<exo> var2);

   <T extends dyc> Optional<T> a(iv var1, dye<T> var2);

   iv a(ehd.a var1, iv var2);
}
