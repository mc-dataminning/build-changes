import java.util.Optional;
import java.util.function.Predicate;

public interface cvx {
   boolean a(hz var1, Predicate<dlj> var2);

   boolean b(hz var1, Predicate<egw> var2);

   <T extends dix> Optional<T> a(hz var1, diz<T> var2);

   hz a(dqv.a var1, hz var2);
}
