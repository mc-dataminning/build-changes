import java.util.function.Function;
import java.util.function.Predicate;

public interface gpd {
   aky a = new aky("textures", ".png");

   void a(aup var1, gpd.a var2);

   gpf a();

   public interface a {
      default void a(alf $$0, aun $$1) {
         this.a($$0, $$2 -> $$2.loadSprite($$0, $$1));
      }

      void a(alf var1, gpd.b var2);

      void a(Predicate<alf> var1);
   }

   public interface b extends Function<gpc, got> {
      default void a() {
      }
   }
}
