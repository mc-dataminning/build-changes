import java.util.Optional;
import java.util.function.Predicate;

public interface czd {
   boolean a(ib var1, Predicate<doz> var2);

   boolean b(ib var1, Predicate<eks> var2);

   <T extends dmf> Optional<T> a(ib var1, dmh<T> var2);

   ib a(dur.a var1, ib var2);
}
