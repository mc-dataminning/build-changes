import javax.annotation.Nullable;

public interface cvl {
   @Nullable
   dcx a(gw var1, dfl var2);

   @Nullable
   default <T extends dcx> dcy<T> a(cpx $$0, dfl $$1, dcz<T> $$2) {
      return null;
   }

   @Nullable
   default <T extends dcx> djx a(aks $$0, T $$1) {
      return $$1 instanceof djx.b<?> $$2 ? $$2.d() : null;
   }
}
