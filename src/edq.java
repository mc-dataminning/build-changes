import com.mojang.serialization.Codec;
import java.util.Optional;

public interface edq {
   Codec<edq> c = md.t.q().dispatch(edq::a, edr::a);
   yt<wg, edq> d = yr.a(me.ab).b(edq::a, edr::b);

   Optional<fcu> a(dhp var1);

   edr<? extends edq> a();
}
