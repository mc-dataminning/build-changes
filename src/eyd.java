import com.mojang.datafixers.util.Either;
import com.mojang.serialization.Codec;
import java.io.IOException;

public interface eyd {
   Codec<eyd> b = eye.f.dispatch(eyd::a, $$0 -> $$0.a().codec());

   eye a();

   Either<eyd.a, eyd.b> b();

   public interface a {
      eme load(apl var1) throws IOException;
   }

   public static record b(agm a) {
   }
}
