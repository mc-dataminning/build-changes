import java.util.List;
import java.util.UUID;

public interface diq {
   diq a = ($$0, $$1, $$2) -> $$0.a($$2x -> $$2x.dm().a($$1, (double)$$2) && !$$2x.f() && !$$2x.P_()).stream().map(blv::cw).toList();
   diq b = ($$0, $$1, $$2) -> {
      elo $$3 = new elo($$1).g((double)$$2);
      return $$0.a(blz.aG, $$3, bml::bx).stream().map(blv::cw).toList();
   };

   List<UUID> detect(and var1, hx var2, int var3);
}
