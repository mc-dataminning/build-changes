import it.unimi.dsi.fastutil.doubles.DoubleList;

interface fce {
   DoubleList a();

   boolean a(fce.a var1);

   int size();

   public interface a {
      boolean merge(int var1, int var2, int var3);
   }
}
