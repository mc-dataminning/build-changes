import com.mojang.serialization.Codec;
import java.util.Optional;

public interface egu {
   Codec<egu> c = mh.t.q().dispatch(egu::a, egv::a);
   ze<wp, egu> d = zc.a(mi.ac).b(egu::a, egv::b);

   Optional<fgc> a(dkj var1);

   egv<? extends egu> a();
}
