import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface doc {
   @Nullable
   ebt a(ebl var1);

   void a(ebl var1, ebt var2);

   LongSet b(ebl var1);

   void a(ebl var1, long var2);

   Map<ebl, LongSet> h();

   void b(Map<ebl, LongSet> var1);
}
