import java.util.Map;
import java.util.stream.Collectors;

public class geo implements AutoCloseable {
   public static final int a = gel.H().stream().mapToInt(gel::I).sum();
   private final Map<gel, fah> b = gel.H().stream().collect(Collectors.toMap($$0 -> (gel)$$0, $$0 -> new fah($$0.I())));

   public fah a(gel $$0) {
      return this.b.get($$0);
   }

   public void a() {
      this.b.values().forEach(fah::g);
   }

   public void b() {
      this.b.values().forEach(fah::h);
   }

   @Override
   public void close() {
      this.b.values().forEach(fah::i);
   }
}
