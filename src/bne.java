import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bne extends bng {
   bnf d();

   @Nullable
   bmz.a c(String var1);

   Set<Pair<String, bom>> e();
}
