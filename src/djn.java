import java.util.Optional;
import java.util.function.Predicate;

public interface djn {
   boolean a(iv var1, Predicate<eao> var2);

   boolean b(iv var1, Predicate<ewv> var2);

   <T extends dxm> Optional<T> a(iv var1, dxo<T> var2);

   iv a(egn.a var1, iv var2);
}
