import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bly extends bma {
   blz d();

   @Nullable
   blt.a c(String var1);

   Set<Pair<String, bng>> e();
}
