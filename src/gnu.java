import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface gnu {
   Collection<ajt> f();

   void a(Function<ajt, gnu> var1);

   @Nullable
   gnj a(gnn var1, Function<gnm, glj> var2, gnr var3, ajt var4);
}
