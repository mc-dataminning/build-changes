import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface etq {
   Codec<etq> c = md.n.q().dispatch(etq::a, etr::codec);

   @Nullable
   tw a(azs var1, @Nullable tw var2);

   etr<?> a();
}
