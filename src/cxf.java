import java.util.Optional;
import java.util.function.Predicate;

public interface cxf {
   boolean a(ib var1, Predicate<dmz> var2);

   boolean b(ib var1, Predicate<ein> var2);

   <T extends dkg> Optional<T> a(ib var1, dki<T> var2);

   ib a(dsm.a var1, ib var2);
}
