import java.util.List;

public enum gcs implements ddt {
   a(ded.c, ded.a, ded.d, ded.b),
   b(ded.e, ded.f, ded.g),
   c(ded.h, ded.i),
   d(ded.j);

   private final List<dee> e;

   private gcs(final dee... $$0) {
      this.e = List.of($$0);
   }

   public List<dee> a() {
      return this.e;
   }
}
