import java.util.Optional;
import java.util.function.Predicate;

public interface dcd {
   boolean a(iz var1, Predicate<dsb> var2);

   boolean b(iz var1, Predicate<enu> var2);

   <T extends dpg> Optional<T> a(iz var1, dpi<T> var2);

   iz a(dxt.a var1, iz var2);
}
