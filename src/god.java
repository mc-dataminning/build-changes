import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface god {
   Collection<ajv> f();

   void a(Function<ajv, god> var1);

   @Nullable
   gns a(gnw var1, Function<gnv, gls> var2, goa var3, ajv var4);
}
