import com.mojang.serialization.Codec;
import com.mojang.serialization.MapCodec;

public interface cum extends cul.b<cuo> {
   Codec<cum> b = mh.aD.q().dispatch(cum::a, $$0 -> $$0);

   MapCodec<? extends cum> a();
}
