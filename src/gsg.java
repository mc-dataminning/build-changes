import java.util.UUID;

public interface gsg {
   void a(UUID var1, gsg.b var2);

   void a(UUID var1, gsg.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
