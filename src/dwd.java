import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dwd<T> extends AutoCloseable {
   CompletableFuture<dvy<T>> a(dbd var1);

   void a(dvy<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
