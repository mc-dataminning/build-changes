import com.mojang.serialization.MapCodec;
import javax.annotation.Nullable;

public class evv implements evx {
   private static final evv b = new evv();
   public static final MapCodec<evv> a = MapCodec.unit(b);

   @Override
   public tz a(azv $$0, @Nullable tz $$1) {
      return new tz();
   }

   @Override
   public evy<?> a() {
      return evy.a;
   }
}
