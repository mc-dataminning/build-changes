import com.mojang.serialization.Codec;
import java.util.Optional;

public interface duv {
   Codec<duv> c = lc.v.q().dispatch(duv::a, duw::a);
   yq<wd, duv> d = yo.a(ld.W).b(duv::a, duw::b);

   Optional<etf> a(czu var1);

   duw<? extends duv> a();
}
