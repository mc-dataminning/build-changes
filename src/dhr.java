import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface dhr {
   @Nullable
   dvf a(dux var1);

   void a(dux var1, dvf var2);

   LongSet b(dux var1);

   void a(dux var1, long var2);

   Map<dux, LongSet> h();

   void b(Map<dux, LongSet> var1);
}
