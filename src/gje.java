import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface gje {
   Collection<aiy> f();

   void a(Function<aiy, gje> var1);

   @Nullable
   git a(gix var1, Function<giw, ggt> var2, gjb var3, aiy var4);
}
