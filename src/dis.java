import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dis<T> extends AutoCloseable {
   CompletableFuture<din<T>> a(cor var1);

   void a(din<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
