import com.mojang.authlib.GameProfile;
import java.util.Map;
import javax.annotation.Nullable;

public record ghd(
   GameProfile a,
   hlh b,
   kf.b c,
   csn d,
   @Nullable String e,
   @Nullable ghn f,
   @Nullable fvi g,
   Map<aku, byte[]> h,
   @Nullable fpr.b i,
   Map<String, String> j,
   alm k
) {
}
