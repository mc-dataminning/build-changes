import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface grk {
   Collection<ale> f();

   void a(Function<ale, grk> var1);

   @Nullable
   gqz a(grd var1, Function<grc, goy> var2, grh var3, ale var4);
}
