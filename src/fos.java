import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fos implements fcq {
   @Nullable
   @Override
   public fcp a(int $$0) {
      return fpc.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
