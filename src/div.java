import java.util.Optional;
import java.util.function.Predicate;

public interface div {
   boolean a(iu var1, Predicate<dzo> var2);

   boolean b(iu var1, Predicate<evv> var2);

   <T extends dwn> Optional<T> a(iu var1, dwp<T> var2);

   iu a(efn.a var1, iu var2);
}
