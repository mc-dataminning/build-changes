import com.mojang.serialization.Codec;
import com.mojang.serialization.MapCodec;
import javax.annotation.Nullable;

public interface bt {
   Codec<bt> a = lh.at.q().dispatch(bt::a, MapCodec::codec);

   MapCodec<? extends bt> a();

   boolean a(brv var1, aqt var2, @Nullable ewu var3);
}
