import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface boi extends bok {
   boj d();

   @Nullable
   bod.a c(String var1);

   Set<Pair<String, bpq>> e();
}
