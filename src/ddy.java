import java.util.Optional;
import java.util.function.Predicate;

public interface ddy {
   boolean a(je var1, Predicate<dua> var2);

   boolean b(je var1, Predicate<eqb> var2);

   <T extends dre> Optional<T> a(je var1, drg<T> var2);

   je a(dzw.a var1, je var2);
}
