import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface dwb {
   Codec<dwb> c = jb.q.q().dispatch(dwb::a, dwc::codec);

   @Nullable
   qr a(apf var1, @Nullable qr var2);

   dwc<?> a();
}
