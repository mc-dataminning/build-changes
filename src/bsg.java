import javax.annotation.Nullable;

public interface bsg {
   xi am();

   default boolean an() {
      return this.ao() != null;
   }

   default xi S_() {
      return this.am();
   }

   @Nullable
   default xi ao() {
      return null;
   }
}
