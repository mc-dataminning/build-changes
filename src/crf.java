import java.util.Optional;
import java.util.function.Predicate;

public interface crf {
   boolean a(ht var1, Predicate<dgb> var2);

   boolean b(ht var1, Predicate<ebe> var2);

   <T extends ddx> Optional<T> a(ht var1, ddz<T> var2);

   ht a(dlk.a var1, ht var2);
}
