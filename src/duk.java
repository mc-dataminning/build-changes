import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface duk<T> extends AutoCloseable {
   CompletableFuture<duf<T>> a(czk var1);

   void a(duf<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
