import java.util.List;

public enum fuj {
   a,
   b;

   private static final List<fuj> c = List.of(values());
   private static final iz.b d = iz.a(kr.au);

   public static is<fuj> a() {
      return new is<>(c).a(a, d);
   }
}
