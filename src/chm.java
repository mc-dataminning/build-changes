import java.util.function.IntFunction;

public enum chm {
   a(0),
   b(1),
   c(2),
   d(3),
   e(4);

   private static final IntFunction<chm> f = axe.a(chm::a, values(), axe.a.b);
   private final int g;

   private chm(final int $$0) {
      this.g = $$0;
   }

   public int a() {
      return this.g;
   }

   public static chm a(int $$0) {
      return f.apply($$0);
   }
}
