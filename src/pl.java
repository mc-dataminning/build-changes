import java.util.concurrent.CompletableFuture;

public class pl extends py<cfg> {
   public pl(mc $$0, CompletableFuture<jo.a> $$1) {
      super($$0, lu.l, $$1);
   }

   @Override
   protected void a(jo.a $$0) {
      this.b(awe.a).a(cfg.b, cfg.c, cfg.d, cfg.e, cfg.f, cfg.g, cfg.h, cfg.i, cfg.j, cfg.k);
      this.b(awe.b).b(awe.a).a(cfg.l);
   }
}
