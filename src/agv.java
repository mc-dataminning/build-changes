import java.util.List;

public enum agv {
   a,
   b,
   c,
   d;

   private static final List<agv> e = List.of(values());
   private static final it.b f = it.a(kc.ar);

   public static in<agv> a() {
      return new in<>(e).a(a, f);
   }
}
