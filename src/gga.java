import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface gga {
   Collection<agt> f();

   void a(Function<agt, gga> var1);

   @Nullable
   gfp a(gft var1, Function<gfs, gdo> var2, gfx var3, agt var4);
}
