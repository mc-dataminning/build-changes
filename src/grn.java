import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface grn {
   Collection<alf> f();

   void a(Function<alf, grn> var1);

   @Nullable
   grc a(grg var1, Function<grf, gpb> var2, grk var3, alf var4);
}
