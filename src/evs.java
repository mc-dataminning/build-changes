import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface evs {
   Codec<evs> c = mg.n.q().dispatch(evs::a, evt::codec);

   @Nullable
   tz a(azv var1, @Nullable tz var2);

   evt<?> a();
}
