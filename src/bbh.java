import com.mojang.serialization.Codec;
import io.netty.buffer.ByteBuf;

public enum bbh {
   a;

   public static final Codec<bbh> b = Codec.unit(a);
   public static final ze<ByteBuf, bbh> c = ze.a(a);
}
