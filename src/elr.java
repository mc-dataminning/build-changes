import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface elr {
   Codec<elr> c = le.p.q().dispatch(elr::a, els::codec);

   @Nullable
   ud a(ayk var1, @Nullable ud var2);

   els<?> a();
}
