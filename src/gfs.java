import java.util.function.Predicate;

@FunctionalInterface
public interface gfs {
   gfs b = $$0 -> $$0x -> true;
   gfs c = $$0 -> $$0x -> false;

   Predicate<dsl> getPredicate(dsm<dfi, dsl> var1);
}
