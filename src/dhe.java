import java.util.function.Predicate;
import javax.annotation.Nullable;

public class dhe implements Predicate<dgw> {
   private final cut a;

   public dhe(cut $$0) {
      this.a = $$0;
   }

   public static dhe a(cut $$0) {
      return new dhe($$0);
   }

   public boolean a(@Nullable dgw $$0) {
      return $$0 != null && $$0.a(this.a);
   }
}
