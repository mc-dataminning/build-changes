import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dqz<T> extends AutoCloseable {
   CompletableFuture<dqu<T>> a(cwi var1);

   void a(dqu<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
