import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dyh<T> extends AutoCloseable {
   CompletableFuture<dyc<T>> a(dcy var1);

   void a(dyc<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
