import java.util.List;

public enum fnu {
   a,
   b;

   private static final List<fnu> c = List.of(values());
   private static final iu.b d = iu.a(kd.as);

   public static io<fnu> a() {
      return new io<>(c).a(a, d);
   }
}
