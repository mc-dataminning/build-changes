import com.mojang.authlib.GameProfile;
import java.util.Map;
import javax.annotation.Nullable;

public record gdl(
   GameProfile a,
   het b,
   ke.b c,
   crq d,
   @Nullable String e,
   @Nullable gdv f,
   @Nullable frw g,
   Map<alj, byte[]> h,
   @Nullable fme.b i,
   Map<String, String> j,
   amb k
) {
}
