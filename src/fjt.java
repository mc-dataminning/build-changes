import it.unimi.dsi.fastutil.ints.IntSet;
import it.unimi.dsi.fastutil.ints.IntSets;
import javax.annotation.Nullable;

public class fjt implements eyd {
   @Nullable
   @Override
   public eyc a(int $$0) {
      return fkd.b;
   }

   @Override
   public IntSet a() {
      return IntSets.EMPTY_SET;
   }
}
