import com.mojang.serialization.Codec;
import java.util.Optional;

public interface dwu {
   Codec<dwu> c = lp.v.q().dispatch(dwu::a, dwv::a);
   zj<ww, dwu> d = zh.a(lq.W).b(dwu::a, dwv::b);

   Optional<evm> a(dbt var1);

   dwv<? extends dwu> a();
}
