import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface ebu<T> extends AutoCloseable {
   CompletableFuture<ebp<T>> a(dgf var1);

   void a(ebp<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
