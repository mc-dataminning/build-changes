import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface ebu {
   Codec<ebu> c = kc.q.q().dispatch(ebu::a, ebv::codec);

   @Nullable
   sd a(atw var1, @Nullable sd var2);

   ebv<?> a();
}
