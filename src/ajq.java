import java.util.List;

public enum ajq {
   a,
   b,
   c,
   d;

   private static final List<ajq> e = List.of(values());
   private static final iz.b f = iz.a(ki.at);

   public static is<ajq> a() {
      return new is<>(e).a(a, f);
   }
}
