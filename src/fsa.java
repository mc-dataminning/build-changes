import java.util.List;

public enum fsa {
   a,
   b;

   private static final List<fsa> c = List.of(values());
   private static final iz.b d = iz.a(ki.at);

   public static is<fsa> a() {
      return new is<>(c).a(a, d);
   }
}
