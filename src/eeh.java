import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface eeh<T> extends AutoCloseable {
   CompletableFuture<eec<T>> a(dic var1);

   void a(eec<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
