import com.mojang.serialization.Codec;
import java.util.Optional;

public interface dvy {
   Codec<dvy> c = le.v.q().dispatch(dvy::a, dvz::a);
   yv<wi, dvy> d = yt.a(lf.W).b(dvy::a, dvz::b);

   Optional<euk> a(dax var1);

   dvz<? extends dvy> a();
}
