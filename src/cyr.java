import javax.annotation.Nullable;

public interface cyr {
   @Nullable
   dgd a(hv var1, dip var2);

   @Nullable
   default <T extends dgd> dge<T> a(csy $$0, dip $$1, dgf<T> $$2) {
      return null;
   }

   @Nullable
   default <T extends dgd> dnb a(amp $$0, T $$1) {
      return $$1 instanceof dnb.b<?> $$2 ? $$2.d() : null;
   }
}
