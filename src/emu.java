import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface emu {
   Codec<emu> c = lp.p.q().dispatch(emu::a, emv::codec);

   @Nullable
   us a(azh var1, @Nullable us var2);

   emv<?> a();
}
