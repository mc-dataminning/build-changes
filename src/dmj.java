import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dmj<T> extends AutoCloseable {
   CompletableFuture<dme<T>> a(csf var1);

   void a(dme<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
