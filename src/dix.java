import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dix<T> extends AutoCloseable {
   CompletableFuture<dis<T>> a(cpi var1);

   void a(dis<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
