import java.util.List;

public enum all {
   a,
   b,
   c,
   d;

   private static final List<all> e = List.of(values());
   private static final kb.b f = kb.a(lu.aA);

   public static ju<all> a() {
      return new ju<>(e).a(a, f);
   }
}
