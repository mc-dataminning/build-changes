import com.mojang.authlib.GameProfile;
import java.util.Map;
import javax.annotation.Nullable;

public record gkq(
   GameProfile a,
   hpa b,
   jt.b c,
   cut d,
   @Nullable String e,
   @Nullable gla f,
   @Nullable fyn g,
   Map<alg, byte[]> h,
   @Nullable fsw.b i,
   Map<String, String> j,
   aly k
) {
}
