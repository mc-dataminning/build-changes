import java.net.URI;
import java.nio.file.Path;
import java.util.Optional;

public record we(xi a, Optional<Path> b, Optional<URI> c) {
   public we(xi $$0) {
      this($$0, Optional.empty(), Optional.empty());
   }
}
