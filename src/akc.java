import java.util.List;

public enum akc {
   a,
   b,
   c,
   d;

   private static final List<akc> e = List.of(values());
   private static final iz.b f = iz.a(kr.au);

   public static is<akc> a() {
      return new is<>(e).a(a, f);
   }
}
