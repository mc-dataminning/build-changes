import java.util.Optional;
import java.util.function.Predicate;

public interface dcc {
   boolean a(iz var1, Predicate<dsa> var2);

   boolean b(iz var1, Predicate<ent> var2);

   <T extends dpf> Optional<T> a(iz var1, dph<T> var2);

   iz a(dxs.a var1, iz var2);
}
