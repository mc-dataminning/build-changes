import java.util.List;

public enum gei implements deh {
   a(der.c, der.a, der.d, der.b),
   b(der.e, der.f, der.g),
   c(der.h, der.i),
   d(der.j);

   private final List<des> e;

   private gei(final des... $$0) {
      this.e = List.of($$0);
   }

   public List<des> a() {
      return this.e;
   }
}
