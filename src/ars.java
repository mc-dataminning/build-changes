import com.mojang.authlib.GameProfile;

public record ars(GameProfile a, int b, aqn c, boolean d) {
   public static ars a(GameProfile $$0, boolean $$1) {
      return new ars($$0, 0, aqn.a(), $$1);
   }
}
