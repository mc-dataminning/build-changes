import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bal extends ban {
   bam d();

   @Nullable
   bag.a c(String var1);

   Set<Pair<String, bbr>> e();
}
