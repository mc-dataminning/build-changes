import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dyv<T> extends AutoCloseable {
   CompletableFuture<dyq<T>> a(ddm var1);

   void a(dyq<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
