import java.util.Set;
import javax.annotation.Nullable;
import org.apache.commons.lang3.tuple.Pair;

public interface bnw extends bny {
   bnx d();

   @Nullable
   bnr.a c(String var1);

   Set<Pair<String, bpe>> e();
}
