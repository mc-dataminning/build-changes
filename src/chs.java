import com.mojang.datafixers.kinds.App;
import java.util.function.Function;

public class chs {
   public static brh<chi> a() {
      return but.a((Function<but.b<chi>, ? extends App<but.c<chi>, buw<chi>>>)($$0 -> $$0.group($$0.c(byr.ad)).apply($$0, $$0x -> ($$0xx, $$1, $$2) -> {
               if (!$$1.eV().b() && !$$1.eV().a(cqn.vp)) {
                  chj.a($$1, true);
                  return true;
               } else {
                  return false;
               }
            })));
   }
}
