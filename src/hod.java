import java.util.UUID;

public interface hod {
   void a(UUID var1, hod.b var2);

   void a(UUID var1, hod.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
