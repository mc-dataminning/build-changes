import com.mojang.serialization.MapCodec;

public class dim extends dlw {
   public static final MapCodec<dim> b = b(dim::new);

   @Override
   public MapCodec<dim> a() {
      return b;
   }

   protected dim(dpx.d $$0) {
      super(djx.b.e, $$0);
   }
}
