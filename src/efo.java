import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface efo<T> extends AutoCloseable {
   CompletableFuture<efj<T>> a(dje var1);

   void a(efj<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
