import com.mojang.serialization.MapCodec;

public interface eoj<SP extends eoi> {
   eoj<eog> a = a("random_spread", eog.a);
   eoj<eof> b = a("concentric_rings", eof.a);

   MapCodec<SP> codec();

   private static <SP extends eoi> eoj<SP> a(String $$0, MapCodec<SP> $$1) {
      return ke.a(mb.P, $$0, () -> $$1);
   }
}
