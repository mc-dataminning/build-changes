import java.util.Optional;
import java.util.function.Predicate;

public interface dbd {
   boolean a(io var1, Predicate<drb> var2);

   boolean b(io var1, Predicate<emu> var2);

   <T extends dog> Optional<T> a(io var1, doi<T> var2);

   io a(dwt.a var1, io var2);
}
