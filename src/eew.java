import java.util.stream.Stream;

public abstract class eew extends eex {
   @Override
   public final Stream<id> a_(eev $$0, axt $$1, id $$2) {
      return this.a($$0, $$1, $$2) ? Stream.of($$2) : Stream.of();
   }

   protected abstract boolean a(eev var1, axt var2, id var3);
}
