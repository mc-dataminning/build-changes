import it.unimi.dsi.fastutil.ints.IntSet;
import javax.annotation.Nullable;

public interface ekk extends AutoCloseable {
   @Override
   default void close() {
   }

   @Nullable
   default ekj a(int $$0) {
      return null;
   }

   IntSet a();
}
