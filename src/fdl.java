import com.mojang.datafixers.util.Either;
import com.mojang.serialization.Codec;
import java.io.IOException;

public interface fdl {
   Codec<fdl> b = fdm.f.dispatch(fdl::a, $$0 -> $$0.a().codec());

   fdm a();

   Either<fdl.a, fdl.b> b();

   public interface a {
      erg load(asf var1) throws IOException;
   }

   public static record b(ajc a) {
   }
}
