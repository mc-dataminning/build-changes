import com.mojang.serialization.Codec;
import java.util.Optional;

public interface dwx {
   Codec<dwx> c = lp.v.q().dispatch(dwx::a, dwy::a);
   zm<wz, dwx> d = zk.a(lq.W).b(dwx::a, dwy::b);

   Optional<evp> a(dbw var1);

   dwy<? extends dwx> a();
}
