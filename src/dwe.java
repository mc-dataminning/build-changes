import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dwe<T> extends AutoCloseable {
   CompletableFuture<dvz<T>> a(dbe var1);

   void a(dvz<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
