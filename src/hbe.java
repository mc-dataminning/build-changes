import java.util.UUID;

public interface hbe {
   void a(UUID var1, hbe.b var2);

   void a(UUID var1, hbe.a var2);

   public static enum a {
      a,
      b,
      c,
      d,
      e;
   }

   public static enum b {
      a,
      b;
   }
}
