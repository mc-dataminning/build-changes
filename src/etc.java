import com.mojang.serialization.Codec;

public abstract class etc {
   public static final Codec<etc> c = md.m.q().dispatch("predicate_type", etc::a, etd::codec);

   public abstract boolean a(dym var1, azs var2);

   protected abstract etd<?> a();
}
