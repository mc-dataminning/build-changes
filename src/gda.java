import java.util.List;

public enum gda {
   a,
   b;

   private static final List<gda> c = List.of(values());
   private static final ke.b d = ke.a(lz.az);

   public static jx<gda> a() {
      return new jx<>(c).a(a, d);
   }
}
