import java.util.List;

public enum ahp {
   a,
   b,
   c,
   d;

   private static final List<ahp> e = List.of(values());
   private static final iu.b f = iu.a(kd.as);

   public static io<ahp> a() {
      return new io<>(e).a(a, f);
   }
}
