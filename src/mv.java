import java.util.concurrent.CompletableFuture;

public class mv extends ne<dtl> {
   public mv(jm $$0, CompletableFuture<hi.b> $$1) {
      super($$0, je.av, $$1);
   }

   @Override
   protected void a(hi.b $$0) {
      this.b(apy.a).a(dtm.a).a(dtm.b).a(dtm.c).a(dtm.d).a(dtm.e).a(dtm.f).a(dtm.g).a(dtm.h).a(dtm.i);
   }
}
