import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface eob {
   Codec<eob> c = lh.p.q().dispatch(eob::a, eoc::codec);

   @Nullable
   uk a(ayt var1, @Nullable uk var2);

   eoc<?> a();
}
