import java.util.Optional;
import java.util.function.Predicate;

public interface csl {
   boolean a(hx var1, Predicate<dhn> var2);

   boolean b(hx var1, Predicate<ecx> var2);

   <T extends dfi> Optional<T> a(hx var1, dfk<T> var2);

   hx a(dmw.a var1, hx var2);
}
