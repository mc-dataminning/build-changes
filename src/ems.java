import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface ems {
   Codec<ems> c = lp.p.q().dispatch(ems::a, emt::codec);

   @Nullable
   us a(azh var1, @Nullable us var2);

   emt<?> a();
}
