import java.io.IOException;
import java.util.concurrent.CompletableFuture;

public interface dit<T> extends AutoCloseable {
   CompletableFuture<dio<T>> a(cos var1);

   void a(dio<T> var1);

   void a(boolean var1);

   @Override
   default void close() throws IOException {
   }
}
