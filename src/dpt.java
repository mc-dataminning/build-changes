import com.mojang.serialization.Codec;
import java.util.Optional;

public interface dpt {
   Codec<dpt> c = kf.v.q().dispatch(dpt::a, dpu::a);
   xo<vb, dpt> d = xm.a(kg.V).b(dpt::a, dpu::b);

   Optional<ens> a(cvn var1);

   dpu<? extends dpt> a();
}
