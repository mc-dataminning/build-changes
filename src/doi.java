import javax.annotation.Nullable;

public interface doi {
   default int a(arp $$0, bul $$1) {
      return 0;
   }

   @Nullable
   est a(arp var1, bul var2, jh var3);

   default doi.a b() {
      return doi.a.b;
   }

   public static enum a {
      a,
      b;
   }
}
