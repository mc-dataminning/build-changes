import java.util.Collection;
import java.util.function.Function;
import javax.annotation.Nullable;

public interface gas {
   Collection<aep> f();

   void a(Function<aep, gas> var1);

   @Nullable
   gah a(gal var1, Function<gak, fyg> var2, gap var3, aep var4);
}
