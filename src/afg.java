import java.util.List;

public enum afg {
   a,
   b,
   c,
   d;

   private static final List<afg> e = List.of(values());
   private static final hu.b f = hu.a(jd.ap);

   public static hn<afg> a() {
      return new hn<>(e).a(a, f);
   }
}
