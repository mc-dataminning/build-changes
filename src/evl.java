import com.mojang.serialization.Codec;
import javax.annotation.Nullable;

public interface evl {
   Codec<evl> c = mf.n.q().dispatch(evl::a, evm::codec);

   @Nullable
   tz a(azv var1, @Nullable tz var2);

   evm<?> a();
}
