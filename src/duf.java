import com.mojang.serialization.Codec;
import java.util.Optional;

public interface duf {
   Codec<duf> c = kt.v.q().dispatch(duf::a, dug::a);
   yg<vt, duf> d = ye.a(ku.W).b(duf::a, dug::b);

   Optional<esj> a(czg var1);

   dug<? extends duf> a();
}
