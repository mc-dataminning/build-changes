import java.util.stream.Stream;

public record ur(aeu a) implements uj {
   @Override
   public Stream<qu> getData(dt $$0) {
      qu $$1 = $$0.l().aG().a(this.a);
      return Stream.of($$1);
   }

   @Override
   public String toString() {
      return "storage=" + this.a;
   }
}
