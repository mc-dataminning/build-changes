import it.unimi.dsi.fastutil.ints.IntSet;
import javax.annotation.Nullable;

public interface eju extends AutoCloseable {
   @Override
   default void close() {
   }

   @Nullable
   default ejt a(int $$0) {
      return null;
   }

   IntSet a();
}
