import javax.annotation.Nullable;

public interface ux<T extends so> {
   void a(si var1);

   void a(T var1);

   default boolean b() {
      return false;
   }

   @Nullable
   default sh c() {
      return null;
   }
}
