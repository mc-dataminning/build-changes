import com.mojang.serialization.Codec;
import io.netty.buffer.ByteBuf;

public enum bas {
   a;

   public static final Codec<bas> b = Codec.unit(a);
   public static final yu<ByteBuf, bas> c = yu.a(a);
}
