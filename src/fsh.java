import java.util.function.Predicate;

@FunctionalInterface
public interface fsh {
   fsh b = $$0 -> $$0x -> true;
   fsh c = $$0 -> $$0x -> false;

   Predicate<dhi> getPredicate(dhj<cva, dhi> var1);
}
